{
  "name": "slp-website",
  "version": "1.0.0",
  "private": true,
  "description": "Schwarzlicht Photography website — locally pre-compiled Tailwind CSS.",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.cjs -i assets/tailwind.input.css -o assets/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3"
  }
}
